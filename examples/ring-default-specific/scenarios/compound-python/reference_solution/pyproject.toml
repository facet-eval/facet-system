[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "notifier"
version = "0.1.0"
description = "Internal team notification router."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
include = ["notifier*"]
